ExUnit.start()

# Start the Repo for tests
# {:ok, _} = Application.ensure_all_started(:ecto_sql)
# {:ok, _} = AlphavantageElixirClient.Repo.start_link()
#
# Ecto.Adapters.SQL.Sandbox.mode(AlphavantageElixirClient.Repo, :manual)
